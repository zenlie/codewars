<?php
    function multiply($a, $b) {
    return $a * $b;
    }
    multiply(1184,109);

    // For terminal. Execute, then see browser and terminal to know the difference.
    // echo multiply(3,4) . "\n"; 
    // echo multiply(3,4);

    // For Html. Execute, then see browser and terminal to know the difference.
    // echo multiply(3,4) . "<br>"; 
    // echo multiply(3,4);
?>